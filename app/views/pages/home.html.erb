<div class="banner" style="background-image: linear-gradient(135deg, rgba(0,0,0,0.65) 0%,rgba(0,0,0,0) 100%), url(<%= image_path 'banner_home.png'%>);">
  <div class="banner-content">
    <h1>Vinyl Love</h1>
    <p>Rent your records today</p>
  </div>
  <%= form_tag records_path, method: :get do %>
  <%= text_field_tag :query,
  params[:query],
  class: "form-control",
    placeholder: "Please enter artist, title or genre"
    %>
    <% end %>
</div>



  <div style="text-align:left"><h4>Most poular records</h4></div>



  <div class="container">
    <div class="row">
      <% @records.each do |record| %>
      <div class="col-xs-12 col-sm-4">
        <div class="card" style="background-image: linear-gradient(135deg, rgba(0, 0, 0, 0.4) 50%, rgba(0, 0, 0, 0.4) 100%), url(<%= image_path 'dsotm.jpg' %>);">
          <div class="card-description">
            <h2>
              <%= link_to record_path(record) do %>
                <%= record.title %> (<%= record.year %>)
              <% end  %>
            </h2>
            <p><%= link_to record.artist, record_path(record) %></p>
            <p> € <%= record.price %>0</p>
          </div>
        </div>
      </div>
       <% end %>
    </div>
  </div>










